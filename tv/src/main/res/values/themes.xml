<resources>
    <!-- Tema base para la TVActivity (Compose for TV).
         Ya no depende de Theme.Leanback: la navegación D-pad ahora
         la maneja Compose (focusRequester / ClickableSurfaceDefaults). -->
    <style name="Theme.SmartHealthTV" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/sh_dark</item>
        <item name="android:colorBackground">@color/sh_dark</item>
    </style>
</resources>
